<wxs src="../common/utils/wxs/namespace.wxs" module="namespace" />
<wxs src="./index.wxs" module="utils" />

<view class="{{ namespace.join('row', { flex }) }}" style="{{ utils.completedStyle(gutter) }}">
  <slot />
</view>
