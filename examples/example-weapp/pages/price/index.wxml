<ti-page options="{{ options }}" bind:change="onChange" style="{{ cssVariable }}">
  <ti-price
    label="销售价"
    unit="$"
    prefix="{{ attrs.prefix }}"
    value="{{ 29.66666 }}"
    radix="{{2}}"
    suffix="{{ attrs.suffix }}"
  />

</ti-page>

